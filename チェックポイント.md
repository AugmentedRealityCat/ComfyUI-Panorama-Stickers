# チェックポイント

この仕上げでは、consumer の移行後に残っている legacy render 依存を shared render 層から順に除去する。

## 現在地点

- [x] `modal pano`
- [x] `cutout preview`
- [x] `preview runtime`
- [x] `preview node`

上の 4 consumer は新 core 経由で描画している。

残っているのは、shared 層にぶら下がった旧 helper の整理だけ。

## 残タスク

### 1. preview helper の整理

- [x] `pano_preview_render.js` から純関数を退避
- [x] `pano_preview_previewnode.js` の `pano_preview_render.js` 依存を除去
- [x] `pano_preview_render.js` を削除

### 2. background pass の legacy 依存除去

- [x] `pano_render_background_pass.js` から `pano_gl_viewport.js` import を除去
- [x] background pass は依存注入がある時だけ外部 renderer を使う形に整理

### 3. legacy viewport helper の削除

- [x] `pano_gl_viewport.js` の実参照を 0 にする
- [x] `pano_gl_viewport.js` を削除

### 4. テスト整理

- [x] 旧 helper 存在前提のテストを新構造へ更新
- [x] core / consumer / cleanup 後の最低限テストを通す

### 5. 最終確認

- [x] consumer から legacy helper 名が 0 件
- [x] shared 層から `pano_gl_viewport.js` 依存が 0 件
- [ ] `coverage` が background のみに作用
- [ ] build 反映

## 原則

- fallback は残さない
- 未実装機能は無理に混ぜない
- 旧 helper を温存して「動いて見える」状態にはしない
